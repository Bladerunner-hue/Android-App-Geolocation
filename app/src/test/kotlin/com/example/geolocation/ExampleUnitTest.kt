package com.example.geolocation

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Local unit test (host JVM) for the GeoJournal package.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
