# TensorFlow-only training environment (keep separate from PyTorch)
tensorflow[and-cuda]>=2.15,<2.21
tensorflow-hub>=0.16
numpy>=1.24
soundfile>=0.12
scipy>=1.11
# Optional Hub uploads (never hardcode tokens)
huggingface-hub>=0.23
